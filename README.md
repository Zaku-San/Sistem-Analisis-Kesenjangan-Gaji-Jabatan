# Salary Equation Management Analysis

## ⚙️ Tools
Make sure the following tools are installed:
- C++ Compiler (GCC / MinGW / g++)
- Windows / Linux / macOS
- Visual Studio Code or Code::Blocks
- Terminal / Command Prompt

---

## 🔍 Check Compiler
Verify that the C++ compiler is installed by running this command:

```bash
g++ --version
```

## ▶️ How to Run the Program
Navigate to Folder Build and run this command:
# CMD:
```bash
program.exe
```

## ▶️ How to Compile the Program
If you modify the source code and want to recompile the program, run the following command from the root project directory:
```bash
g++ src/main.cpp src/Jabatan/jabatan.cpp src/Pegawai/pegawai.cpp -o build/program.exe
```
